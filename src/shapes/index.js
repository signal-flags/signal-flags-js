// src/shapes/index.js

import { defaultShape } from './default-shape';
import { pennant } from './pennant';
import { swallowtail } from './swallowtail';
import { triangle } from './triangle';

export const shapes = {
  default: defaultShape,
  swallowtail,
  pennant,
  triangle,
};
